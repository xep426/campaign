package io.github.xep426.campaign.ui.theme

import androidx.compose.ui.unit.dp

// ═══════════════════════════════════════════════════════════════════════
//  Spacing scale. Every gap in the app is one of these — a stray 13.dp at
//  a call site is how a calm layout turns into an approximate one.
// ═══════════════════════════════════════════════════════════════════════

/** Inside a chip, between an icon and its word. */
val SpaceXs = 6.dp

/** Between a label and the thing it labels. */
val SpaceSm = 12.dp

/** The standard gap: between cards, between rows. */
val SpaceMd = 18.dp

/** Between a heading and its content. */
val SpaceLg = 28.dp

/** Between sections. Silence, doing work. */
val SpaceXl = 40.dp

/** Screen side padding — matches the mock's 30 of 390. */
val ScreenPadding = 26.dp
